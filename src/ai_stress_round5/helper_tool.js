// C:\Users\anand\Repos\Jarvis\src\ai_stress_round5/helper_tool.js

import { console } from 'console';

const log = (message) => {
  console.log(message);
};

const error = (message) => {
  console.error(message);
};

const info = (message) => {
  console.info(message);
};

export { log, error, info };
